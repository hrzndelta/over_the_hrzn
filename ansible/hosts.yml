all:
  children:
    horizon: #Only base_ip.2-base_ip.6 is available in this subnet
      hosts:
        corin:
          ansible_host: {{ base_ip }}.2
        silverwolf:
          ansible_host: {{ base_ip }}.3
        manhattan_cafe:
          ansible_host: {{ base_ip }}.4
      vars:
        ansible_user: hrzndelta
        ansible_connection: ssh
        ansible_ssh_private_key_file: ~/.ssh/id_ed25519_hrzndelta

    gugatan: #Router WebUI is located on base_ip.5 in this subnet
      hosts:
        phoebe:
          ansible_host: {{ base_ip }}.6
      vars:
        ansible_user: itsumi
        ansible_connection: ssh
        ansible_ssh_private_key_file: ~/.ssh/id_ed25519_itsumi
